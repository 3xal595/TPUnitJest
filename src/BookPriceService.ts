export const getAveragePrice = (): number => {
    //console.log(title)
    //console.log(country)
    //let price = 30
    // if(title === 'Les Misérables' && country == "FR ")
    //     price = 20;
    const price = 20
    return price
}